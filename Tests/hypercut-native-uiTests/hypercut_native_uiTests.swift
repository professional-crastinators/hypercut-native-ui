import XCTest
@testable import hypercut_native_ui

final class hypercut_native_uiTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(hypercut_native_ui().text, "Hello, World!")
    }
}
